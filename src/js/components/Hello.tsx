import * as React from "react";
import { Fragment } from 'react'

const Hello = () => (
	<React.Fragment>
		<h1 className="hello">Hello</h1>
		<p>This project is made with React + Webpack + TypeScript + Sass</p>
	</React.Fragment>
)

export default Hello;